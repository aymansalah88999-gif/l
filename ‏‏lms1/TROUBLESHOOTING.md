# 🔧 دليل استكشاف الأخطاء وإصلاحها\n\n## المشاكل التي تم حلها ✅\n\n### 1. ❌ خطأ 404 على `/api/courses/my-created` و `/api/courses/my-enrolled`\n\n**السبب:**\nالـ routes غير مرتبة بشكل صحيح. عندما يكون `Route::get('courses/{course}')` قبل `Route::get('courses/my-created')`، فإن Laravel يعتبر `my-created` و `my-enrolled` كـ ID للكورس.\n\n**الحل:** ✅\n```php\n// الترتيب الصحيح في routes/api.php\nRoute::get('courses', [CourseController::class, 'index']);\nRoute::post('courses', [CourseController::class, 'store']);\nRoute::get('courses/my-enrolled', [CourseController::class, 'myEnrolledCourses']);     // ✅ قبل {course}\nRoute::get('courses/my-created', [CourseController::class, 'myCreatedCourses']);       // ✅ قبل {course}\nRoute::get('courses/{course}', [CourseController::class, 'show']);                     // ✅ بعد المحددة\n```\n\n---\n\n### 2. ❌ خطأ: \"SyntaxError: Unexpected token '<'\"\n\n**السبب:**\nالـ API يعيد HTML (صفحة خطأ) بدلاً من JSON. هذا يعني:\n- الـ auth token غير صحيح\n- المستخدم غير مُصرّح\n- الـ endpoint غير موجود\n\n**الحل:** ✅\n\n#### أولاً: تأكد من استخدام `auth:sanctum`\n```php\n// في routes/api.php\nRoute::middleware('auth:sanctum')->group(function () {\n    Route::get('courses/my-created', [CourseController::class, 'myCreatedCourses']);\n    // جميع الـ routes الأخرى\n});\n```\n\n#### ثانياً: استخدم `axios` بدل `fetch`\n```javascript\n// ❌ لا تستخدم fetch مع localStorage\nconst response = await fetch('/api/courses', {\n    headers: {\n        'Authorization': `Bearer ${localStorage.getItem('auth_token')}`,\n    }\n});\n\n// ✅ استخدم axios (يتعامل مع الـ session تلقائياً)\nimport axios from 'axios';\nconst response = await axios.get('/api/courses');\n```\n\nالفرق:\n- **fetch + localStorage**: يحتاج token في LocalStorage (لكن Inertia لا تستخدمه)\n- **axios + session**: يستخدم الـ cookies التلقائية من Inertia\n\n---\n\n### 3. ❌ الـ localStorage فارغ دائماً\n\n**السبب:**\nInertia.js تستخدم session-based authentication، وليس token-based. لا توجد token تُخزّن في localStorage.\n\n**الحل:** ✅\nاستخدم axios بدل fetch - axios سيُرسل الـ cookies تلقائياً:\n\n```javascript\n// المحاولة الأولى (خطأ)\nconst token = localStorage.getItem('auth_token'); // null!\nconst response = await fetch('/api/courses', {\n    headers: { 'Authorization': `Bearer ${token}` } // مشكلة!\n});\n\n// الطريقة الصحيحة\nimport axios from 'axios';\nconst response = await axios.get('/api/courses'); // ✅ يستخدم الـ session cookies\n```\n\n---\n\n## ✅ التحديثات التي تمت\n\n### الملفات المُحدّثة:\n\n| الملف | التغيير |\n|------|--------|\n| `routes/api.php` | ترتيب الـ routes بشكل صحيح (`my-enrolled` و `my-created` قبل `{course}`) |\n| `resources/js/Pages/Instructor/Dashboard.jsx` | استبدال `fetch` بـ `axios` |\n| `resources/js/Pages/Student/Dashboard.jsx` | استبدال `fetch` بـ `axios` |\n| `resources/js/Pages/Admin/Dashboard.jsx` | استبدال `fetch` بـ `axios` |\n\n---\n\n## 🧪 كيفية الاختبار\n\n### 1. امسح الـ Cache\n```bash\nphp artisan cache:clear\nphp artisan config:clear\nphp artisan view:clear\n```\n\n### 2. أعد تشغيل الـ server\n```bash\n# Terminal 1\nphp artisan serve\n\n# Terminal 2\nnpm run dev\n```\n\n### 3. جرّب التسجيل\n```\nالاسم: أحمد المدرس\nالبريد: ahmed@example.com\nكلمة المرور: password123\nالدور: 👨‍🏫 مدرس\n```\n\n### 4. افتح Network Inspector (F12)\n- اذهب لـ Network tab\n- يجب أن تشوف:\n  - ✅ `GET /api/courses/my-created` → 200 OK\n  - ✅ `GET /api/instructor/stats` → 200 OK\n  - ❌ بدل 404 أو 401\n\n---\n\n## 📝 ملخص الحلول\n\n| المشكلة | السبب | الحل |\n|--------|------|-----|\n| 404 على `/api/courses/my-created` | ترتيب الـ routes خطأ | وضع specific routes قبل dynamic routes |\n| \"Unexpected token '<'\" | الـ API يعيد HTML | استخدام `axios` بدل `fetch` + التأكد من `auth:sanctum` |\n| localStorage فارغ | Inertia تستخدم sessions | لا تحتاج localStorage، axios يتعامل مع الـ cookies |\n| 401 Unauthorized | المستخدم غير مُصرّح | تأكد من تسجيل الدخول وأن `auth:sanctum` موجود |\n\n---\n\n## 🚨 لو استمرت المشاكل\n\n### تحقق من الـ Logs\n```bash\ntail -f storage/logs/laravel.log\n```\n\n### اختبر الـ API مباشرة\n```bash\nphp artisan tinker\n\n# اختبر User\n> $user = User::where('role', 'instructor')->first();\n> $user\n\n# اختبر Courses\n> $user->courses()->get()\n\n# اختبر Stats\n> User::count()\n```\n\n### تأكد من الـ Database\n```bash\nphp artisan migrate:status\nphp artisan migrate  # إذا احتجت\n```\n\n---\n\n## ✅ المتطلبات للعمل بشكل صحيح\n\n```bash\n# في bootstrap.js يجب أن يكون موجود:\nimport axios from 'axios';\nwindow.axios = axios;\n\n# في app.jsx يجب أن يكون موجود:\nimport './bootstrap'; // ✅ يحمل axios\n\n# في الـ dashboards يجب أن يكون موجود:\nimport axios from 'axios';\n```\n\n---\n\n## 🎯 الخطوات النهائية\n\n```bash\n# 1. امسح كل شيء\nphp artisan cache:clear && php artisan config:clear && php artisan view:clear\n\n# 2. أعد تشغيل الـ server\nphp artisan serve\n\n# 3. في terminal آخر\nnpm run dev\n\n# 4. اذهب لـ http://localhost:8000 وسجّل حساب جديد ✅\n```\n\nالآن كل شيء يجب أن يعمل بشكل صحيح! 🚀\n"