# 🔧 إصلاح الخطأ 404 على API Routes\n\n## ❌ المشكلة\n\n```\nGET http://127.0.0.1:8000/api/courses/my-created 404 (Not Found)\nGET http://127.0.0.1:8000/api/instructor/stats 404 (Not Found)\n```\n\n## ✅ السبب والحل\n\n### المشكلة 1: عدم وجود `HasApiTokens` في User Model\n\n**السبب:** Sanctum يحتاج إلى trait `HasApiTokens` لتفعيل API token authentication.\n\n**الحل:**\n```php\n// app/Models/User.php\nuse Laravel\\Sanctum\\HasApiTokens;\n\nclass User extends Authenticatable\n{\n    use HasApiTokens, HasFactory, Notifiable;  // ✅ إضافة HasApiTokens\n}\n```\n\n---\n\n### المشكلة 2: عدم وجود `config/sanctum.php`\n\n**السبب:** Sanctum needs its config file to work properly.\n\n**الحل:** تم إنشاء الملف مع الـ configuration الصحيح.\n\n---\n\n### المشكلة 3: API routes لم تكن مسجّلة في `bootstrap/app.php`\n\n**السبب:** في Laravel 11، الـ API routes يجب أن تكون موجودة في `withRouting()` configuration.\n\n**الحل:**\n```php\n// bootstrap/app.php\nreturn Application::configure(basePath: dirname(__DIR__))\n    ->withRouting(\n        web: __DIR__.'/../routes/web.php',\n        api: __DIR__.'/../routes/api.php',  // ✅ إضافة API routes\n        commands: __DIR__.'/../routes/console.php',\n        health: '/up',\n    )\n```\n\n---\n\n### المشكلة 4: axios لا يرسل الـ cookies\n\n**السبب:** axios بحاجة إلى `withCredentials = true` لإرسال الـ cookies مع الـ requests.\n\n**الحل:**\n```javascript\n// resources/js/bootstrap.js\nwindow.axios.defaults.withCredentials = true;  // ✅ تفعيل الـ cookies\n```\n\n---\n\n## 📋 الملفات المُحدّثة\n\n| الملف | التحديث |\n|------|----------|\n| `app/Models/User.php` | ✅ إضافة `HasApiTokens` trait |\n| `config/sanctum.php` | ✅ إنشاء الملف (جديد) |\n| `bootstrap/app.php` | ✅ إضافة `api:` routes |\n| `resources/js/bootstrap.js` | ✅ إضافة `withCredentials = true` |\n\n---\n\n## 🧪 كيفية الاختبار\n\n### 1. امسح الـ Cache\n```bash\nphp artisan cache:clear\nphp artisan config:clear\nphp artisan route:clear\n```\n\n### 2. أعد تشغيل الـ Server\n```bash\n# Terminal 1\nphp artisan serve\n\n# Terminal 2\nnpm run dev\n```\n\n### 3. جرّب الـ API\n- اذهب لـ http://localhost:8000\n- سجّل كمستخدم جديد (مدرس)\n- افتح DevTools (F12) → Network\n- يجب أن تشوف:\n  - ✅ `GET /api/courses/my-created` → 200\n  - ✅ `GET /api/instructor/stats` → 200\n\n---\n\n## 🔍 التفاصيل التقنية\n\n### كيف يعمل Sanctum مع Inertia\n\n1. **المستخدم يسجل الدخول:**\n   - Laravel ينشئ session cookie\n   - axios يخزن الـ cookie تلقائياً\n\n2. **عند طلب API:**\n   - axios يرسل الـ request مع `withCredentials = true`\n   - Server يتحقق من الـ session cookie\n   - إذا كان صحيح → يعامله كـ authenticated user\n\n3. **Sanctum Middleware:**\n   - `auth:sanctum` يتحقق من:\n     - الـ session (للـ SPA)\n     - أو API token (للـ mobile apps)\n\n---\n\n## ✅ الخطوات التالية\n\nالآن يمكنك:\n\n- ✅ تسجيل مستخدمين جدد\n- ✅ الدخول إلى لوحات التحكم\n- ✅ استدعاء الـ API endpoints\n- ✅ إنشاء وتحديث وحذف الكورسات\n- ✅ عرض الإحصائيات\n\n---\n\n## 🚀 الأوامر النهائية\n\n```bash\n# تنظيف كامل\nphp artisan cache:clear && php artisan config:clear && php artisan route:clear\n\n# تشغيل الـ Server\nphp artisan serve\n\n# في terminal جديد\nnpm run dev\n\n# اذهب لـ http://localhost:8000\n```\n\n**الآن كل شيء يجب أن يعمل!** ✨\n"