# منصة LMS المتكاملة - نظام إدارة الكورسات والامتحانات

## 📚 نظرة عامة

تم بناء نظام إدارة تعليمي متكامل يشبه **Udemy** و **Masterstudypro** يتضمن:

- ✅ إدارة الكورسات (إنشاء، تعديل، حذف)
- ✅ إدارة الدروس بداخل الكورس
- ✅ نظام امتحانات متقدم مع أسئلة متعددة الخيارات
- ✅ نظام تتبع التقدم الدراسي
- ✅ نتائج الامتحانات وتصحيح آلي
- ✅ واجهة رباعية الأدوار (Admin, Instructor, Student)

---

## 🗄️ قاعدة البيانات

### Tables المرئية:

#### 1. **users** (المستخدمون)
```sql
- id, name, email, password, role (student/instructor/admin)
- email_verified_at, created_at, updated_at
```

#### 2. **courses** (الكورسات)
```sql
- id, instructor_id, title, description, image_url
- level (beginner/intermediate/advanced)
- duration_hours, price, students_count, status
- what_you_will_learn, requirements
```

#### 3. **lessons** (الدروس)
```sql
- id, course_id, title, description, content
- video_url, order, is_free, duration_minutes
```

#### 4. **quizzes** (الامتحانات)
```sql
- id, course_id, lesson_id, title, description
- pass_percentage, duration_minutes, show_results
- shuffle_questions, status
```

#### 5. **questions** (الأسئلة)
```sql
- id, quiz_id, type (multiple_choice/true_false/short_answer)
- question_text, order, points, explanation
```

#### 6. **answers** (الإجابات)
```sql
- id, question_id, answer_text, is_correct, order
```

#### 7. **quiz_results** (نتائج الامتحانات)
```sql
- id, student_id, quiz_id, total_points, earned_points
- percentage, passed, duration_seconds
- started_at, completed_at
```

#### 8. **student_answers** (إجابات الطلاب)
```sql
- id, quiz_result_id, question_id, answer_id
- text_answer, is_correct, points_earned
```

#### 9. **student_courses** (تسجيل الطلاب في الكورسات)
```sql
- id, student_id, course_id, progress, completed_lessons
- enrolled_at, completed_at
```

---

## 🔌 API Endpoints

### الكورسات
- `GET /api/courses` - قائمة جميع الكورسات
- `POST /api/courses` - إنشاء كورس جديد
- `GET /api/courses/{id}` - تفاصيل كورس
- `PATCH /api/courses/{id}` - تحديث كورس
- `DELETE /api/courses/{id}` - حذف كورس
- `POST /api/courses/{id}/enroll` - تسجيل الطالب
- `GET /api/my-courses` - كورسات الطالب المسجل فيها
- `GET /api/my-created-courses` - كورسات المدرس

### الدروس
- `GET /api/lessons` - جميع الدروس
- `POST /api/lessons` - إنشاء درس
- `GET /api/lessons/{id}` - تفاصيل درس
- `PATCH /api/lessons/{id}` - تحديث درس
- `DELETE /api/lessons/{id}` - حذف درس
- `GET /api/courses/{courseId}/lessons` - دروس الكورس

### الاختبارات
- `GET /api/quizzes` - جميع الاختبارات
- `POST /api/quizzes` - إنشاء اختبار
- `GET /api/quizzes/{id}` - تفاصيل اختبار
- `PATCH /api/quizzes/{id}` - تحديث اختبار
- `DELETE /api/quizzes/{id}` - حذف اختبار
- `POST /api/quizzes/{id}/start` - بدء الاختبار
- `POST /api/quizzes/{id}/submit-answer` - إرسال إجابة
- `POST /api/quizzes/{id}/complete` - إنهاء الاختبار
- `GET /api/quizzes/{id}/results` - نتائج الاختبار

### الأسئلة
- `GET /api/questions` - جميع الأسئلة
- `POST /api/questions` - إنشاء سؤال
- `GET /api/questions/{id}` - تفاصيل سؤال
- `PATCH /api/questions/{id}` - تحديث سؤال
- `DELETE /api/questions/{id}` - حذف سؤال
- `GET /api/quizzes/{quizId}/questions` - أسئلة الاختبار

---

## 🎨 صفحات React

### صفحات الكورسات
1. **`/courses`** - قائمة جميع الكورسات المتاحة
   - عرض بطاقات الكورسات
   - معلومات المستوى والمدة والسعر
   - زر التسجيل السريع

2. **`/courses/:id`** - تفاصيل الكورس
   - نظرة عامة
   - قائمة الدروس
   - الاختبارات المتاحة
   - معلومات المدرس

### صفحات الاختبارات
1. **`/quizzes/:id/take`** - واجهة الاختبار التفاعلية
   - عرض الأسئلة واحد تلو الآخر
   - شريط تقدم الاختبار
   - زر التنقل بين الأسئلة
   - عرض النتيجة الفورية بعد الإنهاء

---

## 📊 البيانات التجريبية (Seeders)

تم إنشاء بيانات تجريبية:
- **3 مستخدمين**: طالب، مدرس، إداري
- **3 كورسات**: PHP، Laravel، React
- **4 دروس** في الكورس الأول
- **اختبار واحد** مع 3 أسئلة متعددة الخيارات

### تشغيل البيانات التجريبية:
```bash
php artisan db:seed
```

---

## 🚀 الميزات المتقدمة

### 1. نظام التقدم الدراسي
- تتبع عدد الدروس المكتملة
- حساب النسبة المئوية للتقدم تلقائياً

### 2. نظام الامتحانات الذكي
- حساب الدرجات تلقائياً
- تحديد النجاح/الرسوب بناءً على نسبة النجاح
- تسجيل وقت الاختبار

### 3. الأمان والتحكم بالوصول
- التحقق من صلاحيات المستخدم قبل الوصول للكورس
- فقط المدرس يمكنه تعديل كورسه
- فقط الطالب المسجل يمكنه الاختبار

### 4. التخصيص
- ثلاثة مستويات للكورسات (مبتدئ، متوسط، متقدم)
- أسعار مرنة
- دروس مجانية/مدفوعة

---

## 💻 كيفية الاستخدام

### للطلاب:
1. الذهاب إلى `/courses` لعرض الكورسات المتاحة
2. الضغط على زر "التسجيل" للالتحاق بالكورس
3. الذهاب للكورس وعرض الدروس
4. أخذ الاختبارات المتاحة
5. عرض النتائج والنسب المئوية

### للمدرسين:
1. إنشاء كورس جديد من `/instructor/dashboard`
2. إضافة دروس للكورس
3. إضافة اختبارات وأسئلة
4. تتبع نتائج الطلاب

### للإداريين:
1. الوصول إلى `/admin/dashboard` للتحكم الكامل
2. إدارة المستخدمين والأدوار
3. عرض التقارير والإحصائيات

---

## 📁 هيكل الملفات

```
app/
├── Http/
│   ├── Controllers/
│   │   ├── Api/
│   │   │   ├── CourseController.php
│   │   │   ├── LessonController.php
│   │   │   ├── QuizController.php
│   │   │   └── QuestionController.php
│   │   └── ProfileController.php
│   └── Middleware/
│       └── RoleMiddleware.php
├── Models/
│   ├── Course.php
│   ├── Lesson.php
│   ├── Quiz.php
│   ├── Question.php
│   ├── Answer.php
│   ├── QuizResult.php
│   ├── StudentCourse.php
│   ├── StudentAnswer.php
│   └── User.php
└── Providers/

database/
├── migrations/
│   ├── create_courses_table.php
│   ├── create_lessons_table.php
│   ├── create_quizzes_table.php
│   ├── create_questions_table.php
│   ├── create_answers_table.php
│   ├── create_quiz_results_table.php
│   ├── create_student_courses_table.php
│   └── create_student_answers_table.php
└── seeders/
    ├── CourseSeeder.php
    ├── LessonSeeder.php
    └── QuizSeeder.php

resources/js/
├── Pages/
│   ├── Courses/
│   │   ├── Index.jsx
│   │   └── Show.jsx
│   ├── Quizzes/
│   │   └── Take.jsx
│   ├── Student/
│   │   └── Dashboard.jsx
│   ├── Instructor/
│   │   └── Dashboard.jsx
│   └── Admin/
│       └── Dashboard.jsx
└── Layouts/
    ├── AuthenticatedLayout.jsx
    └── GuestLayout.jsx

routes/
├── web.php
├── api.php
└── auth.php
```

---

## 🔐 المصادقة والترخيص

- نظام Sanctum للـ API
- Middleware للتحقق من الدور
- Session-based للويب

---

## 📝 ملاحظات مهمة

- جميع التواريخ مُخزنة في UTC
- الأسعار بصيغة عملة (2 منزلة عشرية)
- النسب المئوية بصيغة decimal (0-100)
- يمكن توسيع النظام بسهولة بإضافة ميزات جديدة

---

## 🎯 الخطوات التالية الممكنة

1. ✅ إضافة نظام الدفع (Stripe, PayPal)
2. ✅ نظام الشهادات عند إتمام الكورس
3. ✅ نظام التعليقات والمناقشات
4. ✅ محتوى وسائط متعددة (صور، فيديوهات)
5. ✅ نظام تقييمات الكورسات
6. ✅ تحديثات في الوقت الفعلي (WebSockets)
7. ✅ تطبيق جوال (React Native)

---

تم الانتهاء! 🎉
